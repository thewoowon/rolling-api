Alembic migrations for rolling-api.

Generate a new migration:
    alembic revision --autogenerate -m "describe change"

Apply migrations:
    alembic upgrade head

Rollback one step:
    alembic downgrade -1
